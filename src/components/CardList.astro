---
import features from '../../data.json';
import Card from './Card.astro';

import "../sass/prueba.scss";



---
<ul class="items">
  {
    features.map((appData) => {
      return (
        <Card
        name={appData.title}
     
         
      
        />
      );
    })
  }
</ul>
